policies:
  - name: RulePolicy
    core_threshold: 0.3
    nlu_threshold: 0.3
    constrain_similarities: true

#policies:
 # - name: EnterpriseSearchPolicy
   # llm:  
     # model_group: gemini-2.0-flash  # تحديد نموذج Gemini-2.0-Flash للاستجابة
   # embeddings:
     # model_group: gemini-2.0-flash  # تحديد نفس النموذج لاستخراج التضمينات (Embeddings)
    #vector_store:
     # type: "faiss"
    #  source: "/workspaces/codespaces-quickstart/docs/sampleDocument.txt"
 # - name: MemoizationPolicy
 # - name: RulePolicy
 # - name: TEDPolicy
  #  max_history: 5
 #   epochs: 100
   # constrain_similarities: true


# The config recipe.
recipe: default.v1

# The assistant project unique identifier
assistant_id: 20250328-213103-adjacent-bagging

# Configuration for Rasa NLU.
language: en

pipeline:
  - name: WhitespaceTokenizer
  - name: RegexFeaturizer
  - name: LexicalSyntacticFeaturizer
  - name: CountVectorsFeaturizer
  - name: DIETClassifier
    epochs: 100
  - name: EntitySynonymMapper
  - name: ResponseSelector
    epochs: 100
  - name: FallbackClassifier
    threshold: 0.3
    ambiguity_threshold: 0.1

# Configuration for Rasa Core.
# The `RulePolicy` and `TEDPolicy` are already included above.
